seed: 42

data:
  path: data/raw/cms_Hbb.csv
  check_hash: true

split:
  train: 0.60
  val: 0.20
  test: 0.20

output:
  metrics: artifacts/metrics.json
  figures: artifacts/figures

models:
  skip_random_forest: false
